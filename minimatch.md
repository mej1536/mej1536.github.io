# Minimatch ReDoS 취약점 해결 요약

GitHub Security에서 알림을 받은 `minimatch` 패키지의 ReDoS(정규 표현식 서비스 거부) 취약점을 해결하기 위해 다음과 같은 작업을 수행했습니다.

## 1. 취약점 개요
- **내용**: `minimatch` 패키지의 `matchOne()` 함수에서 여러 개의 인접하지 않은 GLOBSTAR(`**`) 세그먼트가 사용될 때 발생하는 조합적 백트래킹(combinatorial backtracking) 문제로 인한 ReDoS 취약점입니다.
- **영향**: 악성 패턴을 포함한 입력을 처리할 때 서버의 CPU 자원을 과도하게 소모하게 하여 서비스 중단을 일으킬 수 있습니다.

## 2. 해결 방법
프로젝트의 의존성 구조를 분석한 결과, `minimatch`는 `npm-run-all` 등의 패키지에서 간접적으로 사용되고 있었습니다. 이를 해결하기 위해 다음 명령어를 순차적으로 실행했습니다.

1. **의존성 설치**: `node_modules`가 없는 상태에서 `npm install`을 실행하여 의존성 트리를 실체화했습니다.
2. **보안 패치 적용**: `npm audit fix` 명령어를 통해 보안 취약점이 있는 패키지들을 자동으로 안전한 버전으로 업데이트했습니다.

## 3. 결과 확인
- **최종 설치 버전**: `minimatch@3.1.5`
  - 이 버전은 보고된 ReDoS 취약점(CVE-2022-3517 등)이 수정된 안전한 버전입니다.
- **보안 상태**: `npm audit` 실행 결과, 현재 발견된 취약점은 **0개**입니다.

```bash
$ npm audit
found 0 vulnerabilities
```

## 4. 향후 관리 권장 사항
- 정기적으로 `npm audit`을 실행하여 새롭게 발견되는 취약점을 점검하는 것이 좋습니다.
- 가능한 경우 `npm update`를 통해 의존성을 최신 상태로 유지해 주세요.
